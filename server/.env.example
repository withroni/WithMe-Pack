# Anthropic API key — https://platform.claude.com/settings/keys
# Set this in the Vercel dashboard as an Environment Variable, never in the repo.
ANTHROPIC_API_KEY=sk-ant-...

# Shared secret the app sends as the x-app-token header.
# Generate one with:  openssl rand -hex 32
APP_TOKEN=
